#ifndef LOOPGRAD_HH
#define LOOPGRAD_HH

// self-introduced library
#include "grad_data.hh"

grad_data loopgrad(grad_data first);

// in gradient minimize analisys, loop is calculated until function is minimized. minimizing stop until gradient in given error.

#endif
